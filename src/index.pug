<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
        title Document
    body
        mixin radio(options)
            label(class="radio")    
                input(type="radio", name="" class="radio__btn")
                span(class="radio__name")= options.name
        +radio({name: "Женщина"})
